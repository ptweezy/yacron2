# Rust build artifacts
/target/

# Editor / OS cruft
*.swp
.DS_Store

# Stale Python tooling artifacts (the project is Rust now; ignored if present)
__pycache__/
*.py[cod]
.venv/
.venv*/
.tox/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
/build/
/dist/

# Note: Cargo.lock IS committed (this is an application, not a library).
